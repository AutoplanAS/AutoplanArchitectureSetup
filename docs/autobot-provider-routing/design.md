# Autobot provider routing design (superseded)

> **Status:** Superseded by the provider-agnostic workflow contract

This design was folded into the unified contract at:

- `docs/autobot-provider-agnostic-workflow-contract/design.md`

The unified contract now defines provider routing, phase semantics, human gates, and acceptance
criteria in one place to prevent drift between provider-specific and lifecycle-specific designs.
